%h1 ユーザー詳細ページ
%h3= @user.email
%h2 質問内容
- @user.posts.each do |post|
  %a{:href => "/posts/#{post.id}"}= post.body
  %hr/
%h3 お気に入りしている投稿
- @user.favorited_posts.each do |post|
  = post.user.profile.name
  = post.body

= link_to "ユーザー一覧へ", users_path
= link_to "ホームへ戻る", posts_path
